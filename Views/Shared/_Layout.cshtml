<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sachin Bhasin - @ViewData["Title"]</title>
    <link rel="icon" type="image/png" href="~/Favicon.png">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
    @await RenderSectionAsync("Styles", required: false)
</head>
<body>
    <nav class="pill-nav">
        <button class="hamburger-btn" onclick="toggleMobileMenu()">
            <span></span>
            <span></span>
            <span></span>
        </button>
        <div class="nav-links">
            <a asp-controller="Home" asp-action="index" class="nav-link">Home</a>
            <a asp-controller="Home" asp-action="about" class="nav-link">About</a>
            <a asp-controller="Home" asp-action="collections" class="nav-link">Collections</a>
            <a asp-controller="Home" asp-action="recipes" class="nav-link">Recipes</a>
            <a asp-controller="Home" asp-action="blog" class="nav-link">Blog</a>
            <span class="nav-link coming-soon-btn">Coming Soon™</span>
        </div>
        <div class="mobile-overlay" onclick="toggleMobileMenu()"></div>
    </nav>

    @RenderBody()
    
    <script>
        function toggleMobileMenu() {
            document.querySelector('.hamburger-btn').classList.toggle('active');
            document.querySelector('.nav-links').classList.toggle('active');
            document.querySelector('.mobile-overlay').classList.toggle('active');
            document.body.style.overflow = document.body.style.overflow === 'hidden' ? '' : 'hidden';
        }
        
        // Set active nav link based on current page
        document.addEventListener('DOMContentLoaded', function() {
            const currentPath = window.location.pathname.toLowerCase();
            const navLinks = document.querySelectorAll('.nav-links .nav-link');
            
            navLinks.forEach(link => {
                const href = link.getAttribute('href');
                if (href) {
                    const linkPath = href.toLowerCase();
                    if (currentPath === '/' && linkPath === '/') {
                        link.classList.add('active');
                    } else if (currentPath.includes('index') && linkPath === '/') {
                        link.classList.add('active');
                    } else if (currentPath.includes('about') && linkPath.includes('about')) {
                        link.classList.add('active');
                    } else if (currentPath.includes('collections') && linkPath.includes('collections')) {
                        link.classList.add('active');
                    } else if (currentPath.includes('recipes') && linkPath.includes('recipes')) {
                        link.classList.add('active');
                    } else if (currentPath.includes('blog') && linkPath.includes('blog')) {
                        link.classList.add('active');
                    }
                }
            });
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
